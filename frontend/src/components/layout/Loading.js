import React from "react";
import { CircularProgress } from '@mui/material';

const Loading = () => {
  return (
    <CircularProgress color='veryperi' className='loading' />
  );
};

export default Loading;
